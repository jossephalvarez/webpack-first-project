import React from 'react';
import ReactDom from 'react-dom';

const obj = {a:1}

console.log(obj?.hola);

ReactDom.render(
    <h1>HOLA JOSSEPH ESTO ES WEBPACK</h1>,
    document.getElementById('app'),
)
